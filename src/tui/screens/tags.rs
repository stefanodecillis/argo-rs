//! Tags screen implementation

use ratatui::{layout::Rect, Frame};

use crate::tui::app::App;

/// Render this screen
pub fn render(_frame: &mut Frame, _area: Rect, _app: &App) {
    // Rendering is done in ui.rs for consistency with other screens
}
